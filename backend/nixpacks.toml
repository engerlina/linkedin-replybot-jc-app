[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt", "prisma generate"]

[start]
cmd = "prisma migrate deploy && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
